exports.middleware = {
  checkApiKey: function (action) {
    // if (!apiKey) {
    //   console.error("Set you auth token before using the CLI")
    //   process.exit(1);
    // }
    // action()
  },
}